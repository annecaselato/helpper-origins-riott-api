export enum EnumDecorators {
    CONTROLLER_PREFIX = 'controller_prefix',
    ROUTES = 'routes',
    PRIVATE_ROUTES = 'private_routes',
    PUBLIC_ROUTES = 'public_routes',
    MIDDLEWARE = 'middleware'
}
